pipeline {
	    agent any
            

                 stages {


	            stage ('Build') {
	                steps {
                            
                               dir("cidr_convert_api/java/cidr-api"){ 
                                 script{                      
                               sh 'mvn -B -DskipTests clean package'
                                 }
                                  } 
			         
	                }
	            }
	  
	            stage ('Create docker image') {
	                steps {
	                 echo "creando docker"
	                }
	

	            }
	        }
	}
